import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    top: 0,
    zIndex: 10,
    position: 'absolute',
    width: '100%',
    height: '10%',
    backgroundColor: 'red',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
